At least to start with, going to use bootstrap CDN.
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous">

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.min.js" integrity="sha384-cn7l7gDp0eyniUwwAZgrzD06kc/tftFf19TOAs2zVinnD/C7E91j9yyk5//jjpt/" crossorigin="anonymous"></script>

https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js

We will override colours, and maybe text, but use everything standard else - for now.  Later on we can compile our own bootstrap. In future versions we can write just the CSS and JS we need inline in the header, but for now we are going for quick. 

We have to work out how to make the blog example template into an eleventy template. Should not be too hard. 


We will add amp pages later.  Global instalation

npm init -y
install -g @11ty/eleventy
npx eleventy
npx eleventy --serve
